---
import Layout from '../../layouts/Layout.astro';
import '../../styles/home.css';

const tickerTags = [
	'Digital marketing',
	'Branding',
	'Photography',
	'Video',
	'Editing',
	'Web design',
	'AI integration',
	'Social media',
	'Graphic design',
	'Marketing leadership',
];
const tickerRepeats = [0, 1, 2, 3];

// Vertical KOPEPIX side-mark — alternating solid + outline copies
const verticalCount = 8;
---

<Layout lang="en">
	<a class="skip-link" href="#top">Skip to content</a>
	<div class="cursor-light" aria-hidden="true"></div>

	<header class="masthead">
		<a href="#top" class="brand">
			<span class="brand-mark">KK</span>
			<span class="brand-name">Konsta Kivioja <span class="brand-name-suffix">Portfolio</span></span>
		</a>
		<nav>
			<a href="#profile">About</a>
			<a href="#experience">Experience</a>
			<a href="#skills">Skills</a>
			<a href="#work">Work</a>
			<a href="#contact" class="nav-cta">Get in touch</a>
			<div class="lang-switch" aria-label="Language">
				<a href="/" hreflang="fi">FI</a>
				<span class="lang-sep" aria-hidden="true">·</span>
				<span class="lang-active" aria-current="true">EN</span>
			</div>
		</nav>
	</header>

	<main id="top">
		<!-- ========== HERO ========== -->
		<section class="hero">
			<div class="hero-bg" aria-hidden="true">
				<div class="hero-halo"></div>
			</div>

			<div class="hero-stack">
				<h1 class="hero-layer hero-layer--back" aria-label="Konsta Kivioja Portfolio">
					<span class="hero-line hero-line--top">KONSTA KIVIOJA</span>
					<span class="hero-line hero-line--bottom">PORTFOLIO</span>
				</h1>

				<div class="hero-layer hero-layer--photo" aria-hidden="true">
					<picture>
						<source srcset="/hero-cutout.webp" type="image/webp" />
						<img
							src="/hero-cutout.png"
							onerror="this.src='/hero.jpg'; this.classList.add('hero-photo-fallback');"
							alt=""
							decoding="async"
							fetchpriority="high"
						/>
					</picture>
				</div>

				<div class="hero-layer hero-layer--front" aria-hidden="true">
					<span class="hero-line hero-line--top">KONSTA KIVIOJA</span>
					<span class="hero-line hero-line--bottom" aria-hidden="true">PORTFOLIO</span>
				</div>
			</div>

			<div class="hero-ticker" aria-hidden="true">
				<div class="hero-ticker-track">
					{tickerRepeats.map(() => tickerTags.map((tag) => (
						<span class="ticker-tag">{tag}</span>
					)))}
				</div>
			</div>
		</section>

		<div class="post-hero">
			<aside class="vertical-mark" aria-hidden="true">
				{Array.from({ length: verticalCount }).map((_, i) => (
					<span class={`vertical-text ${i % 2 === 0 ? 'vertical-text--solid' : 'vertical-text--outline'}`}>KOPEPIX</span>
				))}
			</aside>

		<!-- ========== ABOUT ========== -->
		<section id="profile" class="band band--standfirst" data-section="01" data-section-name="About">
			<h2 class="display" data-reveal>About</h2>
			<p class="standfirst" data-reveal data-reveal-tier="primary">
				<span class="word">A</span> <span class="word">marketing</span> <span class="word">all-rounder.</span>
				<span class="word">Plenty</span> <span class="word">of</span>
				<span class="word">experience</span> <span class="word">working</span>
				<span class="word">with</span> <span class="word">sales</span>
				<span class="word">teams</span> <span class="word">to</span>
				<span class="word">hit</span> <span class="word">shared</span>
				<span class="word">goals.</span> <span class="word">Skilled</span>
				<span class="word">at</span> <span class="word">building</span>
				<span class="word">marketing</span> <span class="word">plans</span>
				<span class="word">that</span> <span class="word">align</span>
				<span class="word">with</span> <span class="word">company</span>
				<span class="word">strategy.</span> <span class="word">I've</span>
				<span class="word">led</span> <span class="word">marketing</span>
				<span class="word">for</span> <span class="word">a</span>
				<span class="word">growth-stage</span> <span class="word">company</span>
				<span class="word">and</span> <span class="word">its</span>
				<span class="word">subsidiary</span> <span class="word">brand</span>
				<span class="word">successfully</span> <span class="word">with</span>
				<span class="word">limited</span> <span class="word">resources.</span>
				<span class="word">On</span> <span class="word">AI,</span>
				<span class="word">I</span> <span class="word">know</span>
				<span class="word">the</span> <span class="word">current</span>
				<span class="word">possibilities</span> <span class="word">well</span>
				<span class="word">and</span> <span class="word">have</span>
				<span class="word">tested</span> <span class="word">dozens</span>
				<span class="word">of</span> <span class="word">AI</span>
				<span class="word">tools.</span> <span class="word">I</span>
				<span class="word">offer</span> <span class="word">marketing</span>
				<span class="word">services</span> <span class="word">under</span>
				<span class="word">the</span> <span class="word">name</span>
				<span class="word">KopePix.</span>
			</p>
			<div class="standfirst-actions">
				<a href="#work" class="button primary">View work</a>
				<a href="#contact" class="button ghost">Get in touch</a>
			</div>
		</section>

		<!-- ========== 03 — SKILLS ========== -->
		<section id="skills" class="band band--split" data-section="03" data-section-name="Skills">
			<header class="band-head" data-reveal>
				<span class="band-num">03 / Skills</span>
				<h2 class="display">What I do</h2>
			</header>
			<div class="band-body two-col">
				<div class="col" data-reveal>
					<h3 class="col-label">Marketing</h3>
					<ul class="chips">
						<li>Marketing leadership</li>
						<li>AI in marketing</li>
						<li>Website design and maintenance</li>
						<li>Social media</li>
						<li>Social media advertising</li>
						<li>Video and photo editing</li>
						<li>Graphic design</li>
						<li>And a lot more</li>
					</ul>
				</div>
				<div class="col" data-reveal>
					<h3 class="col-label">Software</h3>
					<ul class="chips">
						<li>Photoshop</li>
						<li>Lightroom</li>
						<li>DaVinci Resolve</li>
						<li>Canva</li>
						<li>ChatGPT &amp; Gemini</li>
						<li>Midjourney</li>
						<li>Claude Code</li>
					</ul>
				</div>
			</div>
		</section>

		<!-- ========== 04 — SELECTED WORK ========== -->
		<section id="work" class="band band--work" data-section="04" data-section-name="Work">
			<header class="band-head" data-reveal>
				<span class="band-num">04 / Selected work</span>
				<h2 class="display">Recent<br />projects</h2>
			</header>
			<div class="work-list">
				<a class="work" href="https://paljuvuokrausoulu.fi/" target="_blank" rel="noopener" data-reveal>
					<span class="work-index">N° 01</span>
					<div class="work-meta">
						<span class="work-tag">Web · SEO · GEO</span>
						<h3>Website redesign</h3>
						<p>Redesigning the paljuvuokrausoulu.fi website plus SEO and GEO optimization.</p>
					</div>
					<span class="work-cta">
						Open site
						<svg width="14" height="14" viewBox="0 0 14 14" aria-hidden="true">
							<path d="M3 11L11 3M11 3H4M11 3V10" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round"></path>
						</svg>
					</span>
				</a>
				<a class="work" href="https://www.youtube.com/@KDFloorball" target="_blank" rel="noopener" data-reveal>
					<span class="work-index">N° 02</span>
					<div class="work-meta">
						<span class="work-tag">Video editing</span>
						<h3>Aftermovies for floorball national teams</h3>
						<p>Match aftermovies edited for a YouTube channel. Plus around 1,000 goal highlight reels edited.</p>
					</div>
					<span class="work-cta">
						Open channel
						<svg width="14" height="14" viewBox="0 0 14 14" aria-hidden="true">
							<path d="M3 11L11 3M11 3H4M11 3V10" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round"></path>
						</svg>
					</span>
				</a>
				<a class="work" href="https://youtu.be/rQkSkqKakRA?feature=shared" target="_blank" rel="noopener" data-reveal>
					<span class="work-index">N° 03</span>
					<div class="work-meta">
						<span class="work-tag">Video</span>
						<h3>Employee career story production</h3>
						<p>Script, filming, and editing.</p>
					</div>
					<span class="work-cta">
						Watch video
						<svg width="14" height="14" viewBox="0 0 14 14" aria-hidden="true">
							<path d="M3 11L11 3M11 3H4M11 3V10" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round"></path>
						</svg>
					</span>
				</a>
				<a class="work" href="https://anfra-aidat.fi/" target="_blank" rel="noopener" data-reveal>
					<span class="work-index">N° 04</span>
					<div class="work-meta">
						<span class="work-tag">Branding</span>
						<h3>Anfra fences brand and website</h3>
						<p>Anfra fences brand, website, and advertising built from scratch.</p>
					</div>
					<span class="work-cta">
						Open site
						<svg width="14" height="14" viewBox="0 0 14 14" aria-hidden="true">
							<path d="M3 11L11 3M11 3H4M11 3V10" stroke="currentColor" stroke-width="1.5" fill="none" stroke-linecap="round"></path>
						</svg>
					</span>
				</a>
				<div class="work" data-reveal>
					<span class="work-index">N° 05</span>
					<div class="work-meta">
						<span class="work-tag">AI · Image library</span>
						<h3>AI-generated brand image library</h3>
						<p>A brand image library created with Midjourney, a recognizable style without the cost of a photo shoot.</p>
					</div>
					<span class="work-cta inert">Case study coming</span>
				</div>
				<div class="work" data-reveal>
					<span class="work-index">N° 06</span>
					<div class="work-meta">
						<span class="work-tag">Web · AI</span>
						<h3>This site, coded with Claude Code</h3>
						<p>Portfolio site built with Astro and coded entirely using Claude.</p>
					</div>
					<span class="work-cta inert">You're on it</span>
				</div>
			</div>
		</section>

		<!-- ========== 04 — EXPERIENCE & EDUCATION ========== -->
		<section id="experience" class="band band--split" data-section="04" data-section-name="Experience">
			<header class="band-head" data-reveal>
				<span class="band-num">04 / Experience</span>
				<h2 class="display">Career<br />and education</h2>
			</header>
			<div class="band-body two-col">
				<div class="col" data-reveal>
					<h3 class="col-label">Experience</h3>
					<ol class="entries">
						<li>
							<span class="entry-role">Marketing Manager</span>
							<span class="entry-org">Anfra Oy · 10/2022 – 2/2026</span>
						</li>
						<li>
							<span class="entry-role">Warehouse Worker</span>
							<span class="entry-org">Pro Nutrition Finland Oy · 8/2021 – 5/2022</span>
						</li>
						<li>
							<span class="entry-role">Sales</span>
							<span class="entry-org">Lämpöykkönen Oy · 2/2020 – 8/2020</span>
						</li>
						<li>
							<span class="entry-role">Newspaper Distributor</span>
							<span class="entry-org">Kaleva · 11/2018 – 2/2020</span>
						</li>
						<li>
							<span class="entry-role">Flue Coater · Sales</span>
							<span class="entry-org">Fixi Oy · 8/2017 – 10/2018</span>
						</li>
						<li>
							<span class="entry-role">Telemarketer</span>
							<span class="entry-org">Pisaron Oy</span>
						</li>
					</ol>
				</div>
				<div class="col" data-reveal>
					<h3 class="col-label">Education</h3>
					<ol class="entries">
						<li>
							<span class="entry-role">Digital Marketing Fundamentals</span>
							<span class="entry-org">Google Digital Garage · 2022</span>
						</li>
						<li>
							<span class="entry-role">Digital Excellence Academy</span>
							<span class="entry-org">Saranen Consulting · 2022</span>
						</li>
						<li>
							<span class="entry-role">High school diploma</span>
							<span class="entry-org">Haukipudas Upper Secondary School · 2011–2015</span>
						</li>
						<li>
							<span class="entry-role">Tutor and PE tutor training</span>
							<span class="entry-org">Haukipudas Upper Secondary School</span>
						</li>
					</ol>
				</div>
			</div>
		</section>

		<!-- ========== 05 — CONTACT ========== -->
		<section id="contact" class="band band--contact" data-section="05" data-section-name="Contact">
			<div class="contact-stage" data-reveal>
				<span class="band-num">05 / Contact</span>
				<h2 class="display contact-display">Let's work<br />together?</h2>
				<p class="contact-lead">
					Whatever kind of marketing project you have in mind, feel free to reach out.
				</p>
				<div class="contact-grid">
					<a class="contact-card" href="mailto:konstakivioja@gmail.com">
						<span class="contact-label">Email</span>
						<strong>konstakivioja@gmail.com</strong>
						<span class="contact-arrow" aria-hidden="true">→</span>
					</a>
					<a class="contact-card" href="tel:+358401409823">
						<span class="contact-label">Phone</span>
						<strong>+358 40 140 9823</strong>
						<span class="contact-arrow" aria-hidden="true">→</span>
					</a>
				</div>
			</div>

			<footer class="colophon">
				<span>© 2026 Konsta Kivioja · Kopepix</span>
				<a href="#top">Back to top ↑</a>
				<a class="license-link" href="https://creativecommons.org/licenses/by-nc/4.0/" target="_blank" rel="noopener">CC BY-NC 4.0</a>
			</footer>
		</section>
		</div>
	</main>
</Layout>

<script type="module">
	const hover = window.matchMedia('(hover: hover)').matches;
	const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

	// ---------- Cursor light ----------
	const light = document.querySelector('.cursor-light');
	if (light && hover) {
		let tx = 0, ty = 0, cx = 0, cy = 0, raf = 0;
		const move = () => {
			cx += (tx - cx) * 0.18;
			cy += (ty - cy) * 0.18;
			light.style.transform = `translate(${cx}px, ${cy}px)`;
			raf = requestAnimationFrame(move);
		};
		document.addEventListener('pointermove', (e) => {
			tx = e.clientX;
			ty = e.clientY;
			if (!light.classList.contains('active')) light.classList.add('active');
			if (!raf) raf = requestAnimationFrame(move);
		});
		document.addEventListener('pointerleave', () => light.classList.remove('active'));
	}

	// ---------- Stagger word indices for standfirst reveal ----------
	document.querySelectorAll('.standfirst .word').forEach((w, i) => {
		w.style.setProperty('--i', i);
	});

	// ---------- Hero ticker — scroll-driven horizontal translate ----------
	const tickerTrack = document.querySelector('.hero-ticker-track');
	if (tickerTrack && !reducedMotion) {
		let tRaf = 0;
		const TICKER_MULT = 0.35;
		const updateTicker = () => {
			tickerTrack.style.setProperty('--ticker-x', window.scrollY * TICKER_MULT);
			tRaf = 0;
		};
		window.addEventListener(
			'scroll',
			() => {
				if (!tRaf) tRaf = requestAnimationFrame(updateTicker);
			},
			{ passive: true }
		);
		updateTicker();
	}

	// ---------- Reveal on scroll ----------
	const reveals = document.querySelectorAll('[data-reveal]');
	if ('IntersectionObserver' in window) {
		const io = new IntersectionObserver(
			(entries) => {
				entries.forEach((entry) => {
					if (entry.isIntersecting) {
						entry.target.classList.add('in-view');
						io.unobserve(entry.target);
					}
				});
			},
			{ threshold: 0.12, rootMargin: '0px 0px -10% 0px' }
		);
		reveals.forEach((el) => io.observe(el));
	} else {
		reveals.forEach((el) => el.classList.add('in-view'));
	}

	// ---------- Interactive headings: split into chars + react to cursor ----------
	const displays = document.querySelectorAll('.display');
	displays.forEach((el) => {
		const wrap = (node) => {
			if (node.nodeType === 3) {
				const frag = document.createDocumentFragment();
				const text = node.nodeValue;
				for (const ch of text) {
					const span = document.createElement('span');
					if (ch === ' ' || ch === ' ') {
						span.className = 'char space';
						span.innerHTML = '&nbsp;';
					} else if (ch === '­') {
						span.className = 'char';
						span.textContent = '­';
					} else {
						span.className = 'char';
						span.textContent = ch;
					}
					frag.appendChild(span);
				}
				node.parentNode.replaceChild(frag, node);
			} else if (node.nodeType === 1 && node.tagName !== 'BR') {
				Array.from(node.childNodes).forEach(wrap);
			}
		};
		Array.from(el.childNodes).forEach(wrap);
	});

	if (hover && !reducedMotion) {
		const ROOT_MARGIN = 140;
		const MAX_LIFT = 18;
		const MAX_SCALE = 1.18;

		displays.forEach((el) => {
			const chars = el.querySelectorAll('.char');
			let raf = 0;
			let mx = 0, my = 0;

			const apply = () => {
				chars.forEach((ch) => {
					const r = ch.getBoundingClientRect();
					const cx = r.left + r.width / 2;
					const cy = r.top + r.height / 2;
					const dx = mx - cx;
					const dy = my - cy;
					const dist = Math.sqrt(dx * dx + dy * dy);
					if (dist < ROOT_MARGIN) {
						const t = 1 - dist / ROOT_MARGIN;
						const tx = -dx * 0.12 * t;
						const ty = -MAX_LIFT * t;
						const scale = 1 + (MAX_SCALE - 1) * t;
						ch.style.transform = `translate(${tx.toFixed(2)}px, ${ty.toFixed(2)}px) scale(${scale.toFixed(3)})`;
						if (t > 0.55) ch.classList.add('hot');
						else ch.classList.remove('hot');
					} else if (ch.style.transform) {
						ch.style.transform = '';
						ch.classList.remove('hot');
					}
				});
				raf = 0;
			};

			el.addEventListener('pointermove', (e) => {
				mx = e.clientX;
				my = e.clientY;
				if (!raf) raf = requestAnimationFrame(apply);
			});

			el.addEventListener('pointerleave', () => {
				chars.forEach((ch) => {
					ch.style.transform = '';
					ch.classList.remove('hot');
				});
			});
		});
	}
</script>
